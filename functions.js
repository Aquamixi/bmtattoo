
// Adiciona a classe para animar o efeito no carregamento
window.addEventListener('load', () => {
    const heroButton = document.getElementById('heroButton');
    heroButton.classList.add('loaded');
});